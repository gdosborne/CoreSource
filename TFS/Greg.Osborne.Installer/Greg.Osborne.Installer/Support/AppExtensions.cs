using System;
using System.Windows.Media;
using System.Xml.Linq;
using GregOsborne.Application.Media;

namespace Greg.Osborne.Installer.Support {
	public static class AppExtensions {
		//public static T GetItemValue<T>(this XElement parent, string xmlName, T defaultValue) {
		//	var val = parent.Element(xmlName) == null ? defaultValue.ToString() : parent.Element(xmlName).Value;
		//	if (typeof(T) == typeof(Color)) {
		//		return (T)(object)val.ToColor();
		//	} else {
		//		return (T)Convert.ChangeType(val, typeof(T));
		//	}
		//}
	}
}
